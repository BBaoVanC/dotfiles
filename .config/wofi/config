allow_images=true
layer=overlay

# Keybinds

key_up=Ctrl-k
key_down=Ctrl-j
key_left=Ctrl-h
key_right=Ctrl-l
key_pgup=Ctrl-b
key_pgdn=Ctrl-f

key_expand=Tab
key_copy=Ctrl-c

# NOTE FOR LATER:
# wofi uses GIO to launch apps, and GIO has the terminals hardcoded.
# 2.75 isnt out yet, but it adds `xdg-terminal-exec` as a valid terminal
# https://todo.sr.ht/~scoopta/wofi/176
# https://todo.sr.ht/~scoopta/wofi/73
# https://gitlab.gnome.org/GNOME/glib/-/blob/ae65753b16edb34eef71b377abb158c74069ce72/gio/gdesktopappinfo.c#L2639
# https://gitlab.gnome.org/GNOME/glib/-/blob/2.74.2/gio/gdesktopappinfo.c#L2657

# don't set DRI_PRIME=1 because the non default GPU is the iGPU
drun-disable_prime=true
