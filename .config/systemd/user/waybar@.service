# Adapted to a multi-instance unit from /usr/lib/systemd/user/waybar.service
[Unit]
Description=Highly customizable Wayland bar for Sway and Wlroots based compositors
Documentation=https://github.com/Alexays/Waybar/wiki/
PartOf=%i-session.target
After=%i-session.target
Requisite=%i-session.target

[Service]
ExecStart=/usr/bin/waybar --config %h/.config/waybar/config.%i
ExecReload=kill -SIGUSR2 $MAINPID
Restart=on-failure
Slice=session.slice

[Install]
WantedBy=%i-session.target
