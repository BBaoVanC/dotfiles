#!/bin/sh

selection=$(echo """襤 Shutdown
ﰇ Reboot""" | dmenu -i -l 10 -p 'power:')

case $selection in
    "襤 Shutdown")
        shutdown now
        ;;
    "ﰇ Reboot")
        reboot
        ;;
esac
